import Foundation

public enum GameConstants {
    public enum History {
        public static let maxRecentHands: Int = 5  // Maximum number of hands to keep in recent history
    }
    
    public enum Math {
        public static let targetNumber: Int = 24  // The target number players need to reach
    }
} 